# DECISIONS.md

> Folio 프로젝트의 주요 설계 결정사항.
> "왜 그렇게 했는가"를 기록하여 시간 지나도 컨텍스트 유지.

## 포맷

각 결정은 다음 구조로 기록:

```
## D-XXX: 결정 제목
**날짜**: YYYY-MM-DD
**버전**: v0.XX
**상태**: 적용 중 / 대체됨 / 보류
**결정**: 무엇을 선택했는가
**대안**: 검토했던 다른 선택지
**근거**: 왜 이 선택인가
**트레이드오프**: 포기한 것 / 리스크
```

---

## D-001: 기본 모델로 Gemini 3.1 Pro 채택

**날짜**: 2026-04-13
**버전**: v0.12
**상태**: 적용 중

**결정**: 기본 대화 모델을 Gemini 3.1 Pro로 설정 (유저가 명시적으로 변경하지 않는 한).

**대안**:
- Claude Opus 4.6 — 품질 최상이지만 비용 부담
- Claude Sonnet 4.6 — 중간 품질/비용
- Gemini 2.5 Pro — 저렴하지만 캐릭터성 유지율 50%

**근거**:
- 5모델 × 4캐릭터 실험에서 Gemini 3.1 Pro가 평균 유지율 95% 달성
- Opus(100%) 대비 5%p 손해, Output 비용 52% 절감 ($25 → $12)
- 포트폴리오 프로토타입 단계에서 비용-품질 균형점으로 최적

**트레이드오프**:
- 문학적 비유가 중요한 캐릭터(박재헌)에서 미세한 품질 저하 가능
- 향후 Phase 3 "모델 라우팅 가설" 구현 시 캐릭터별 최적 모델 자동 배정 예정

---

## D-002: 3층 프롬프트 아키텍처 도입

**날짜**: 2026-04-10
**버전**: v0.10
**상태**: 적용 중

**결정**: 프롬프트를 characters / common / models 3층으로 분리.

**대안**:
- 캐릭터별 단일 파일 (기존 방식) — 공통 규칙 중복
- 2층 (캐릭터 + 공통) — 모델별 보정 불가

**근거**:
- 모델별 편향 패턴 가설(가설 2)에서 모델마다 고유 위반 패턴 발견
- 모든 캐릭터 프롬프트에 모델별 보정을 중복 삽입하는 건 유지보수 불가
- 레이어 분리로 각 변경의 영향 범위 최소화

**트레이드오프**:
- 프롬프트 조합 로직(buildSystemPrompt.js) 복잡도 증가
- 디버깅 시 최종 프롬프트 확인 과정 필요

---

## D-003: 플랫폼 레벨 콘텐츠 등급 시스템

**날짜**: 2026-04-14
**버전**: v0.13
**상태**: 적용 중

**결정**: 캐릭터 레벨 Safety 토글 대신 플랫폼 레벨 `rating` 필드 (all/toggleable/adult_only).

**대안**:
- 캐릭터별 Safety 토글만 유지 — 일관성 부족
- 유저 프로필에 "성인 콘텐츠 허용" 단일 플래그 — 캐릭터별 세밀 제어 불가

**근거**:
- 성인 콘텐츠 미허용 유저에게 `adult_only` 캐릭터 노출 자체를 차단해야 함
- 캐릭터 레벨 토글은 유저가 매번 개별 설정해야 하는 부담
- 플랫폼 레벨 필터링으로 "노출 자체 차단" + "허용 유저는 캐릭터별 토글" 이중 레이어

**트레이드오프**:
- 기존 캐릭터 config 마이그레이션 필요
- `toggleable` 캐릭터는 여전히 유저가 개별 토글 관리

---

## D-004: username 변경 불가 (immutable)

**날짜**: 2026-04-20
**버전**: v0.20
**상태**: 적용 중

**결정**: `users.username` (@아이디)은 가입 후 변경 불가.

**대안**:
- 자유롭게 변경 가능 — URL 깨짐, 팔로우 관계 훼손
- 기간 제한 변경 (6개월에 1회 등) — 복잡도 증가
- 어드민 승인 하에 변경 — 운영 부담

**근거**:
- `/creator/@:username` URL에 직접 사용되어 변경 시 외부 공유 링크 깨짐
- 팔로우, 책갈피 등 관계 데이터가 username 기반으로 연결
- 인스타그램, 트위터 등 표준 크리에이터 플랫폼 패턴 따름
- 닉네임은 별도로 자유롭게 변경 가능하므로 유저 표현 자유는 보장

**트레이드오프**:
- 가입 시 고민 없이 정한 아이디를 바꾸고 싶을 때 불만 발생 가능
- → 대신 가입 시 "변경 불가" 안내 문구 명확히 표시

---

## D-005: 회원가입에 이메일 + @아이디 + 닉네임 3개 요구

**날짜**: 2026-04-20
**버전**: v0.20
**상태**: 적용 중

**결정**: 가입 필수 입력값을 이메일(로그인용, 비공개) + @아이디(공개 식별자) + 닉네임(표시명)으로 분리.

**대안**:
- 이메일 + 닉네임만 (@아이디는 마이페이지에서 설정) — 설정 안 하는 유저 발생
- 닉네임 = @아이디 (통합) — URL에 한글/이모지 사용 불가
- 이메일만 (익명 가입) — 크리에이터 인지도 형성 불가

**근거**:
- 크리에이터 중심 플랫폼으로 포지셔닝하려면 고유 식별자(@아이디) 필수
- 역할 분리: 이메일=로그인, @아이디=공개 식별자, 닉네임=자유로운 표시명
- 인스타그램 표준 패턴 (@username + display name)
- 나중에 설정하게 두면 설정 안 하는 유저 발생 → 가입 시 필수

**트레이드오프**:
- 가입 폼 입력 항목 증가로 이탈률 상승 가능
- → @아이디 필드에 실시간 중복 확인 UI로 이탈 최소화

---

## D-006: 로그인 시 이메일 / @아이디 둘 다 허용

**날짜**: 2026-04-20
**버전**: v0.20
**상태**: 적용 중

**결정**: 로그인 입력 필드를 "이메일 또는 @아이디" 통합으로 처리.

**대안**:
- 이메일 전용 — 유저가 @아이디 기억해서 입력 시도 → 실패
- @아이디 전용 — 이메일 기억하는 유저 불편
- 탭 분리 — UI 복잡도 증가

**근거**:
- 유저마다 기억하는 값이 다름 (이메일 vs @아이디)
- 입력값 형식으로 서버가 자동 판별 가능 (@ 포함 or 영문만 → username, 아니면 email)
- 단일 필드가 UX 가장 간결

**트레이드오프**:
- 서버 로직에 분기 처리 필요 (identifier → email or username 판별)

---

## D-007: 어드민 대시보드 같은 도메인 유지 (현재)

**날짜**: 2026-04-12
**버전**: v0.11
**상태**: 적용 중 (향후 분리 예정)

**결정**: 어드민을 `/admin/*` 경로로 유저 앱과 동일 도메인에 배포.

**대안**:
- 서브도메인 (`admin.folio.com`) — 보안/배포 분리
- 별도 레포지토리 — 완전 분리

**근거**:
- 포트폴리오 프로토타입 단계에서 단순성 우선
- 어드민도 role 기반 접근 제어로 보안 기본 확보
- 실제 프로덕션 시 서브도메인 분리 계획 PRD에 명시

**트레이드오프**:
- 어드민 경로 노출 (외부에서 `/admin` 시도 가능)
- 유저 앱과 어드민 배포 사이클 묶임
- → 프로덕션 단계에서 서브도메인 분리 예정

---

## D-008: 빌더 제작 캐릭터만 크리에이터 @아이디 표시

**날짜**: 2026-04-20
**버전**: v0.20
**상태**: 적용 중

**결정**: 캐릭터 카드와 프로필의 크리에이터 @아이디 표시는 유저 빌더 제작 캐릭터에만 적용. 프리빌트 캐릭터(이화, 영일, 지세현, 박재헌)는 제외.

**대안**:
- 프리빌트 캐릭터도 `@midnight_atelier` 등 가상 크리에이터 표시
- 전체 캐릭터에 크리에이터 미표시

**근거**:
- 프리빌트 캐릭터는 "Folio 공식 캐릭터"로 포지셔닝
- 크리에이터 시스템은 유저 생성 콘텐츠(UGC) 활성화용
- 단, `/creator/@:username` 프로필 구조는 프리빌트에도 가상 크리에이터로 동작

**트레이드오프**:
- 프리빌트 캐릭터의 크리에이터 정체성 없음 → 향후 공식 크리에이터 계정 생성 시 추가

---

## D-009: 댓글 시스템은 UI만 준비, 기능 후속 단계

**날짜**: 2026-04-21
**버전**: v0.21
**상태**: 적용 중

**결정**: 캐릭터 프로필에 COMMENTS 탭을 두되, 현재는 "준비 중" placeholder만 표시. 실제 기능은 후속 구현.

**대안**:
- 댓글 기능 즉시 구현 — 스코프 초과
- 댓글 탭 제거 — UI 로드맵 시각화 불가

**근거**:
- 포트폴리오 관점에서 "계획된 기능"을 시각화하는 가치
- UI 자리 잡아두면 실제 구현 시 구조 이미 있음
- placeholder로 "어떤 기능이 올지" 전달 가능

**트레이드오프**:
- "미완성" 인상 줄 수 있음 → BETA 배지 등으로 명확화

---

## D-010: REVENUE.PREVIEW에 실제 금액 대신 placeholder

**날짜**: 2026-04-21
**버전**: v0.21
**상태**: 적용 중

**결정**: 마이페이지의 수익 섹션에 하드코딩된 더미 금액(142,300원 등) 대신 "준비 중" 안내만 표시.

**대안**:
- 더미 금액 유지 — 포트폴리오 리뷰어 오해 가능
- 섹션 자체 제거 — 향후 확장 계획 시각화 불가

**근거**:
- 포트폴리오 단계에서 하드코딩 숫자는 "실제 작동하는 기능"으로 오해 유발
- BETA 배지 + "준비 중" 안내로 "계획된 기능"임을 명확히 전달
- 실제 결제 시스템 연동 시 자연스럽게 교체 가능

**트레이드오프**:
- 현재 UI에 시각적 임팩트 감소 → BETA 배지 디자인으로 보완

---

## D-011: 동적 SQL 쿼리는 db/index.js 헬퍼 함수로 분리

**날짜**: 2026-04-23
**버전**: v0.23
**상태**: 적용 중

**결정**: strftime format·테이블명·컬럼명처럼 SQL 파라미터(`?`)로 처리할 수 없는 동적 값이 필요한 쿼리는, `db.prepare()`를 `db/index.js` 내부의 헬퍼 함수로 분리하고 라우터에서는 그 함수만 호출한다.

**대안**:
1. 라우터에서 `db.prepare()` 직접 호출 허용 (기존 admin.js 방식) — 컨벤션 위반
2. 가능한 조건 조합을 모두 별도 prepared statement로 등록 — 조합 수가 많을 때 비현실적
3. ORM 도입 — 프로토타입 규모에서 오버엔지니어링

**근거**:
- SQLite의 `strftime` 첫 번째 인자(format string)와 테이블명·컬럼명은 SQL 파라미터화 불가
- 라우터에서 `db.prepare()` 호출을 허용하면 컨벤션 경계가 무너짐
- `db/index.js`에 격리하면 SQL이 한 곳에 모이고 라우터는 함수 호출만 담당

**트레이드오프**:
- prepared statement 캐싱 불가 (호출마다 새로 compile) → 어드민 전용 저빈도 쿼리라 성능 영향 미미
- 헬퍼 함수 인자에 테이블명·컬럼명을 그대로 받으므로 SQL injection 주의 필요 → 라우터에서 whitelist 검증 후 전달

---

## D-012: 보안 프레임워크 일괄 적용 (helmet + rate-limit)

**날짜**: 2026-04-23
**버전**: v0.23
**상태**: 적용 중

**결정**: helmet(보안 헤더), express-rate-limit(속도 제한), cookie secure/httpOnly 플래그를 일괄 적용. csurf는 제외하고 SameSite=lax로 CSRF 방어.

**대안**:
1. csurf 적용 — 2023년 deprecated, 유지보수 중단. SameSite=lax가 동일 기능 커버하므로 제외
2. rate-limit 인메모리 대신 Redis — 프로토타입 규모에서 오버엔지니어링, 단일 서버 배포라 불필요
3. check-username에 authLimiter(10회) 동일 적용 — 실시간 타이핑 UX 파괴, 별도 30회 limiter 분리

**근거**:
- 포트폴리오 제출 전 기본 보안 체계 확립
- helmet은 설정 한 줄로 10+ 헤더를 일괄 적용, 비용 대비 효과 최대
- SameSite=lax는 CSRF의 가장 큰 공격 벡터(cross-origin form POST)를 차단

**트레이드오프**:
- CSP `unsafe-inline` 허용 — 현재 인라인 스크립트/스타일 다수 사용, nonce 방식으로 전환 시 리팩터링 필요
- rate-limit 스토어가 인메모리 — 서버 재시작 시 초기화, 분산 환경 미지원

---

## D-013: 환경 분리 전략

**날짜**: 2026-04-23
**버전**: v0.25
**상태**: 적용 중

**결정**: Development (로컬) + Production (Railway) 2단계 구조 유지.

**대안**:
- Staging 환경 추가 — Railway staging 인스턴스 별도 배포, PR 머지 전 staging에서 수동 QA

**근거**:
- 1인 개발 + 포트폴리오 단계라 QA 팀 부재
- 배포 직후 수동 검증으로 대체 가능
- 추가 비용/복잡도 대비 실익 낮음

**트레이드오프**:
- Production 직접 배포 → 미검증 버그가 실사용자에게 즉시 노출될 수 있음
- 현재는 실사용자 규모가 작아 리스크 수용 가능

**향후 확장**:
- 실사용자 발생 시 Staging 도입
- 기능 플래그로 staging-only 기능 분리
- E2E 테스트 자동화 시 Staging 필수화

---

## D-014: 프론트엔드 프레임워크 — Vanilla JS 유지 및 React 마이그레이션 준비 전략

**날짜**: 2026-04-26
**버전**: v0.25
**상태**: 적용 중 (Vanilla JS 유지), React 마이그레이션 보류

**결정**: 현재 Vanilla JS SPA 구조를 유지. React 마이그레이션은 실사용자 발생 또는 팀 합류 시점으로 연기.

**대안**:
- React + Vite 즉시 마이그레이션
- Next.js App Router로 전면 재작성
- 점진적 마이그레이션 (화면 단위 React 컴포넌트 교체)

**근거**:
- 포트폴리오 프로토타입 단계에서 마이그레이션 실익 낮음
- 예상 소요: 2–3주 (현실적으론 1달 이상 — 전형적인 과소 추정 패턴)
- 점진적 마이그레이션보다 실사용자 발생 시 Next.js로 처음부터 새로 짜는 게 기술 부채 적음

**트레이드오프**:
- Vanilla JS 유지 → 팀 합류 시 온보딩 비용 증가 가능
- React 즉시 전환 → 현재 작동하는 기능들에 버그 유입 리스크

**마이그레이션 시 핵심 위험 포인트**:
1. **전역 상태 의존성** — `_currentUser`, `currentCharacter` 등 전역 변수가 암묵적으로 공유됨. React Context/Zustand로 전환 시 참조 관계 전수 파악 필요
2. **`innerHTML` 템플릿 리터럴 규모** — app.js 전체에서 HTML을 문자열로 조립. JSX 변환 중 `escapeHtml`, 조건 분기, 이벤트 핸들러 혼재로 버그 유입 가능성 높음
3. **채팅 스트리밍** — `fetch` + `ReadableStream` 직접 파싱. React 상태 업데이트 사이클과 충돌 가능 (`useEffect` 클린업, 언마운트 중 setState 에러)

**마이그레이션 전 준비 작업 (지금부터 가능)**:
1. 전역 상태를 단일 `state` 객체로 통합 → Context 전환 단순화
2. `onclick` 인라인 핸들러 → 함수 분리 (P2 접근성 작업과 병행 가능)
3. `app.js`(4000줄)를 화면 단위 파일로 분리 → React 컴포넌트 파일 구조와 바로 매핑됨

**향후 전환 시 권장 스택**:
- Next.js App Router (점진적 마이그레이션 대신 전면 재작성)
- 상태 관리: Zustand (규모 대비 가벼움)
- 스타일: 기존 CSS 토큰 유지 또는 Tailwind

---

## D-015: 장기기억 아키텍처 — 세션 간 유저 기억 요약 저장

**날짜**: 2026-04-27
**버전**: v0.26
**상태**: 적용 중

**결정**:
- `memories` 테이블에 (user_id, character_id) 당 1개의 요약 텍스트 저장
- 새 세션 시작 시 이전 세션들을 경량 모델로 요약 → 시스템 프롬프트 `Long-term Memory` 섹션에 주입
- 요약 생성은 백그라운드 비동기 실행 (첫 메시지 레이턴시 없음)
- 로그인 유저 전용 (게스트는 쿠키 수명 짧아 실익 없음)

**대안**:
1. RAG + 벡터 DB (Pinecone 등) — 수백 세션 중 관련 기억만 검색. 정확도 높으나 외부 서비스 의존·비용 증가. B안으로 보류.
2. 세션별 개별 메모리 — 세션 수 증가 시 프롬프트 길이 폭증. 기각.
3. 클라이언트 측 저장 — 기기 간 동기화 불가. 기각.

**요약 모델 선택 기준**:
- 세션 모델의 제공사를 따라 경량 모델 사용
  - Gemini 세션 → `gemini-2.5-flash`
  - Anthropic 세션 → `claude-haiku-4-5-20251001`
- 실패 시 반대 제공사로 자동 폴백 → 어느 API 키 조합이든 동작 보장

**트레이드오프**:
- 요약 품질이 AI에 의존 → 간헐적으로 부정확한 기억 가능성
- 기억은 새 세션부터 반영 (현재 세션 중간에는 갱신 안 됨)
- 단일 요약 덮어쓰기 → 오래된 기억 소실 (B안 도입 전까지 한계)

---

## D-016: 포트폴리오 iframe 임베딩 — CSP frame-ancestors 허용

**날짜**: 2026-04-27
**버전**: v0.26
**상태**: 적용 중

**결정**:
- helmet의 `X-Frame-Options` (`frameguard`) 비활성화
- CSP `frame-ancestors` 디렉티브로 포트폴리오 도메인 (`gabby-pm-portfolio.vercel.app`) 단독 허용

**근거**:
- `X-Frame-Options`와 `frame-ancestors` 동시 설정 시 브라우저마다 처리 방식 상이 → CSP로 통합 제어가 표준
- `frame-ancestors`는 허용 출처를 명시적으로 화이트리스트 지정 → `X-Frame-Options: ALLOWALL` 대비 안전
- 자사 도메인(`'self'`) + 포트폴리오 도메인만 허용, 나머지 전체 차단 유지

**트레이드오프**:
- 구형 브라우저(IE 11 이하)는 `frame-ancestors` 미지원 → `X-Frame-Options` 없으면 모든 곳에서 임베딩 가능. 대상 유저 기준 무시 가능한 수준.

---

## D-017: SQLite 영속 저장 — Railway Volume + DB_PATH env 패턴

**날짜**: 2026-05-04
**버전**: v0.30
**상태**: 적용 중 (PostgreSQL 마이그레이션 — D-014 Phase 2 — 전 임시 해결)

**결정**:
- `db/index.js`에 `DB_PATH` 환경변수 도입 (default: `path.join(__dirname, 'chat.db')`)
- 프로덕션(Railway): Volume을 `/data`에 마운트 + env `DB_PATH=/data/chat.db`
- 로컬 dev: env 미지정 → 기존 `db/chat.db` 사용

**대안**:
1. PostgreSQL 즉시 마이그레이션 — D-014 Phase 2의 정석 해결책. 그러나 코드 전체(prepared statement) 재작성 필요, 1-2주 소요. v0.30 시점엔 시급하게 데이터 손실부터 막아야 함
2. Railway Volume을 `/app/db`에 마운트 — 첫 시도였으나 `db/index.js` 코드 디렉토리와 충돌, 크래시 (L-016 참고)
3. SQLite 파일을 git에 커밋 — 프로덕션 DB 동기화 문제, 멀티 인스턴스 불가, 기각

**근거**:
- **데이터 손실 차단이 최우선 (L-015)** — 매 배포마다 유저 계정 / 대화 / 알림 / 장기기억 전부 삭제되던 상황을 즉시 멈춰야 함
- Volume 마운트는 5분 안에 적용 가능한 가장 빠른 대응
- 코드 변경 최소화 (DB_PATH env만 추가)

**트레이드오프**:
- SQLite 단일 파일 한계 그대로 — 동시성, 백업, 복제 불리
- Railway 단일 인스턴스 종속 — 멀티 리전 / 로드밸런싱 시 한계
- D-014 Phase 2(PostgreSQL 전환)으로 가야 진짜 프로덕션 그레이드

**구현 디테일**:
- `fs.mkdirSync(DB_DIR, { recursive: true })`로 빈 볼륨 첫 마운트 대비
- 마운트 경로는 코드 디렉토리와 분리 (`/data` / `/app/db` 등 코드 위치 회피, L-016)

---

## D-018: 프로덕션 부트스트랩 — 임시 HTTP 엔드포인트 + env-secret 게이트

**날짜**: 2026-05-05
**버전**: v0.30
**상태**: 적용 중 (필요 시에만 활성)

**결정**:
- 프로덕션 컨테이너에 직접 접근 불가 (Railway 무료/Pro 모두 web shell 없음, `railway run`은 로컬 실행 — L-017)
- 직접 DB 조작이 필요한 1회성 작업(첫 admin 승격 등)을 위해 임시 HTTP 엔드포인트 패턴 도입
- `POST /api/auth/_bootstrap-admin`
  - `BOOTSTRAP_SECRET` env 없으면 404 반환 (기본 비활성)
  - 헤더 `X-Bootstrap-Secret` 일치 시만 실행
  - 사용 후 Railway에서 env 삭제 → 자동 비활성화

**대안**:
1. Railway Pro 플랜 + web shell — 비용 발생, 일시적 작업에 과함
2. 코드에 admin 시드값 하드코딩 — 시크릿 노출 위험, 환경 분리 불가
3. 마이그레이션 스크립트 (서버 시작 시 실행) — 멱등성 / 트리거 시점 제어 어려움
4. PostgreSQL + 외부 DB 클라이언트 접근 — D-014 Phase 2 완료 후 가능

**근거**:
- 최소 권한 / 일시 활성 원칙: 평소엔 404, env 설정해야만 작동
- 코드에 시크릿 미포함 — 시크릿은 Railway env에만 존재
- 사용 후 env 삭제로 즉시 비활성화 가능 (deploy 없이)
- 코드 자체는 main에 남아있어도 해롭지 않음 (env 없으면 동작 X)

**트레이드오프**:
- 코드 흔적이 main에 남음 — 누군가 BOOTSTRAP_SECRET 추측 시도 가능
- 완화: 32자+ 랜덤 헤더값 사용, 사용 직후 env 즉시 삭제
- 더 강화: 사용 횟수 카운터, 시간제한 등 — 현재 미적용 (포트폴리오 단계)

**사용 후 보안 체크리스트**:
- [ ] `BOOTSTRAP_SECRET` env 삭제 확인
- [ ] 부트스트랩 호출 로그 (Railway Logs) 확인
- [ ] 승격된 admin 계정 비밀번호 즉시 변경

---

## D-019: React (Next.js) 마이그레이션 핵심 설계 결정

**날짜**: 2026-05-27 ~ 2026-08-30
**버전**: Phase A
**상태**: **적용 완료** (cutover 완료)

> Vanilla SPA(`public/`) → Next.js 14 App Router + TypeScript(`web/`) 이식 중 내린 결정 묶음.

**cutover 완료 (2026-08-30)**
- 레거시 SPA 제거 — `public/index.html`·`admin.html`·`js/`·`css/`. `public/`에는 정적 자원(`favicon.ico`·`icons/`·`images/`·`uploads/`)만 남는다. Next가 `/images`·`/icons`·`/uploads`를 이 서버로 프록시하므로 계속 필요하다
- Express는 API + 정적 자원 전용. 남은 HTML 요청은 `FRONTEND_ORIGIN`으로 302 (path 보존). 미설정 시 리다이렉트 대신 404 JSON — 잘못된 도메인으로 보내는 것보다 안 보내는 쪽이 안전
- 구 `adminPageGuard`(서버사이드 role 검증)는 `web/middleware.ts`가 대체. Edge에서 `/api/auth/me`로 role 확인 후 비어드민 차단
- CSP `frame-ancestors`를 `next.config.mjs headers()`로 이관 — helmet 헤더는 이제 HTML에 안 붙는다 (D-016 유지)
> 상세 작업 일지는 `web/MIGRATION_HISTORY.md` (ML-001~ML-014).

**결정 1 — 상태 전달은 URL query 우선, store는 1-hop만**
- 채팅 진입에 필요한 persona/safety/characterId를 인트로 → 페르소나 → 챗으로 전달
- 원본은 글로벌 변수(`window._persona` 등). React에선 `useChatPrepStore`(zustand)가 persona setup → chat 한 hop만 담당하고, 그 외(safety, sessionId)는 URL query/param이 source of truth
- **근거**: 새로고침/뒤로가기/직접 URL 진입 안전. in-memory store는 reload 시 소실 (ML-009, Codex F5)

**결정 2 — SSE 스트리밍 (token-by-token)**
- `POST /api/chat`·`/regenerate`를 JSON 일괄 응답 → SSE로 전환 (`lib/streamReply.js`)
- 프론트는 `streamSSE` AsyncGenerator + requestAnimationFrame typewriter smoothing
- **트레이드오프**: 원본 `public/` SPA는 JSON 기대 → SSE 응답에 깨짐. 마이그레이션 완료 후 `public/` 폐기 전제

**결정 3 — 라우트 인증 정책 매트릭스 + `useRequireAuth`**
- public: `/`, `/character/[id]`, `/explore`, `/login`, `/signup`, `/reset-password`
- login required: `/persona/*`, `/character/[id]/chat`, `/history`, `/mypage`
- 공통 훅으로 일관 적용 (ML-014)

**결정 4 — username 정책 완화 (immutable → 변경 허용)**
- 과거 CLAUDE.md 절대금지 7은 "username immutable". 그러나 원본 정보수정 모달 + `PATCH /api/auth/me`는 변경 허용이 이미 구현됨
- **결정**: 원본대로 변경 허용 유지. 단 변경 시 DB unique 재검증 필수. CLAUDE.md 7번 문구 완화
- **근거**: 1:1 이식 원칙 + 현실 코드와 정책 정합

**결정 5 — 보안 리뷰(Codex)의 묶음 적용 금지**
- 외부 QA finding은 severity·영향범위로 분리. critical만 즉시, UX/API 정책 변경 동반은 반박 → 사용자 확정 후 적용 (ML-013)
- 기존 백엔드·배포 인프라 이슈(파일 영속화, Builder 강화 등)는 마이그레이션 스코프와 분리 → `docs/PRODUCTION_PLAN.md` 9.5 백로그

**대안 검토**:
- 상태 전달에 Context/전역 store만 사용 → reload 취약. URL 병행이 우수
- streaming 없이 일괄 응답 유지 → 체감 대기 길어 UX 저하

---

## D-020: 모델별 보정은 Layer 3 자유 마크다운 + 어드민 편집 (구조화 필드 아님)

**날짜**: 2026-06-05
**버전**: post-마이그레이션
**상태**: 적용 중

**결정**: 모델별 문체·분위기·진전도·깊이 등 지시는 기존 Layer 3(`prompts/models/{id}.md`)에
**자유 마크다운**으로 작성하고, 어드민 `/admin/models`에서 편집. `buildSystemPrompt`의 주입 로직은
변경하지 않음(이미 charPrompt + guardrails + 모델보정 + safety + persona 순으로 주입 중).

**대안**:
- 구조화 필드(문체/분위기/진전도/깊이 별도 입력 → 서버가 .md 조립): 일관되나 스키마 고정·유연성 낮음
- 코드에 모델별 분기 하드코딩: 비개발자 편집 불가, 배포 필요

**근거**: 기존 Layer 3가 이미 작동 → 주입 로직 무변경으로 리스크 0. 프롬프트는 본질적으로 자유서술이라
마크다운이 유연. 캐릭터 system.md 편집과 동일 패턴이라 UX 일관.

**트레이드오프**: 형식 강제 없음(가이드 템플릿으로 보완). 추후 구조화 필요 시 스키마 위에 얹어 확장 가능.

**연관**: 챗봇 모델 정규 목록을 `lib/chatModels.js`로 단일화(ALLOWED_MODELS Set들과 프론트 MODELS가
동일 목록을 따라야 함 — Gemini는 provider 분기 set 누락 시 무음 오라우팅 위험).

---